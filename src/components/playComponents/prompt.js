import React from "react";
import { StyleSheet, Text, View, Pressable, TextInput, Image } from "react-native";

const Prompt = () => {
	return (
		<View>
			<Text>I'm a prompt!</Text>
		</View>
	);
};

const styles = StyleSheet.create({});

export default Prompt;
